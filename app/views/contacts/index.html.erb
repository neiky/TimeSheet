<div class="page-header">
  <h2>Kontaktliste</h2>
</div>

<%= link_to 'Neuen Kontakt anlegen', new_contact_path, :class => 'btn btn-primary' %>

<div class="row content">
  <div class="span4">
    <table class="table table-striped">
      <thead>
      </thead>

      <tbody>
      <% @contacts.each do |contact| %>
        <tr id="contact_<%= contact.id %>" class="unselected">
          <td><%= link_to contact, contact, :remote => :true %></td>
          <td style="text-align: right"><%= link_to contact.client, client_path(contact.client) %></td>
        </tr>
      <% end %>
      </tdody>
    </table>
  </div>
  <div id="contact_details" class="span8">
    Wählen Sie einen Eintrag.
  </div>

</div>